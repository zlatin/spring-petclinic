pipeline{
    agent any

    stages{
        stage("Deploy artifact"){
            steps {
                script{
                    withAWS(credentials: 'aws_login'){
                        def images = ecrListImages(repositoryId: '353770932280')
                        echo("${images}")
                    }
                }
            }
        }
    }

}