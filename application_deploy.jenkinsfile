pipeline{
    agent any

    stages{
        stage("Deploy artifact"){
            steps {
                script{
                    withAWS(credentials: 'aws_login'){
                        images = ecrListImages(repositoryName: 'petclinic')
                        echo("${images}")
                    }
                }
            }
        }
    }

}